import { Routes } from '@angular/router';
import { ImageViewerComponent } from './image-viewer-capability/components/image-viewer/image-viewer.component';


export const routes: Routes = [
    {path:'',component:ImageViewerComponent}
];
