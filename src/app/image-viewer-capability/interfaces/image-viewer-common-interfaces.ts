export interface ImageTransformationConfigurations {
    /** Flag to determine if grid lines need to be shown */
    showGridLines: boolean;
    /** Transition styles */
    transformationStyle: string
}
